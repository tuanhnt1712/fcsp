<div class="right_col" role="main">
  <div class="page-title">
    <div class="title_left">
      <ol class="breadcrumb-arrow">
        <li><a href="#"><%= t ".courses" %></a></li>
      </ol>
    </div>

    <h4><%= t ".all_courses" %></h4>

    <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
          <div class="x_title">
          </div>
          <div class="x_content">
            <table id="datatable" class="table table-striped table-bordered">
              <thead>
                <tr>
                  <th class="stt">#</th>
                  <th><%= t ".name" %></th>
                  <th><%= t ".status" %></th>
                  <th><%= t ".start_date" %></th>
                  <th><%= t ".end_date" %></th>
                </tr>
              </thead>

              <tbody>
                <% user.courses.each do |course| %>
                  <tr>
                    <td></td>
                    <td><%= link_to course.name, "#" %></td>
                    <td>
                      <% if course.status == Settings.courses.init %>
                        <span class="label label-warning"><%= course.status %></span>
                      <% elsif course.status == Settings.courses.in_progress %>
                        <span class="label label-success"><%= course.status %></span>
                      <% elsif course.status == Settings.courses.finished %>
                        <span class="label label-danger"><%= course.status %></span>
                      <% else %>
                        <span class="label label-default"><%= course.status %></span>
                      <% end %>
                    </td>
                    <td>
                      <% if course.start_date.present? %>
                        <%= l course.start_date, format: :date_month_year %>
                      <% else %>
                        <%= t ".no_data" %>
                      <% end %>
                    </td>
                    <td>
                      <% if course.start_date.present? %>
                        <%= l course.end_date, format: :date_month_year %>
                      <% else %>
                        <%= t ".no_data" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
